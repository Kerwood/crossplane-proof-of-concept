---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: xappregistrationdefaults.defaults.fn.crossplane.io
spec:
  group: defaults.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: XAppRegistrationDefaults
    listKind: XAppRegistrationDefaultsList
    plural: xappregistrationdefaults
    singular: xappregistrationdefaults
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          appRegNamePrefix:
            description: Example is an example field. Replace it with whatever input
              you need. :)
            type: string
          clusterIssuerUrl:
            type: string
          crossplaneServicePrincipalId:
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          providerConfigRef:
            properties:
              kind:
                type: string
              name:
                type: string
            required:
            - kind
            - name
            type: object
        required:
        - clusterIssuerUrl
        - providerConfigRef
        type: object
    served: true
    storage: true
